import React from 'react';

const ProductRating = ({ rating }) => {
  // Logic to render product rating (e.g., stars, numeric rating)
  return <div className="product-rating">{rating}</div>;
};

export default ProductRating;
